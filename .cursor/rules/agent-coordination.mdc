---
description: Multi-agent lanes and freeze rules so parallel Cursor agents do not clobber each other.
alwaysApply: true
---

# Agent coordination (product freeze)

Canonical workspace is `/WSL/recon-pipeline` on branch `private`.
Do **not** edit `/home/moirax/recon-pipeline`. That tree is stale.

Do not `git push`, rewrite git history, or commit `recon/**` scan artifacts unless the user explicitly asked **this** chat to do so.

## Current lanes (2026-09-08 23:28)

All tracked chats have `turn_ended`. Do not push unless the user asks.

| Lane | Status | Instruction |
|------|--------|-------------|
| Post-port vhost | **DONE** | Vhost after ports. |
| Readable / raw UI | **DONE** | Static dashboard. |
| Operator STOP | **DONE** | Do not restart preview. |
| Warehouse / notify | **FROZEN** | Do not reopen. |

## Merge rules

1. One writer per file at a time. If your file is not in your exclusive lane, **stop**.
2. Do not kill the preview process on port 8765 unless you own that lane and must restart it.
3. If you detect another agent has uncommitted edits in your files, rebase on current disk — do not overwrite blindly.
4. `app.js` IDs stay stable.

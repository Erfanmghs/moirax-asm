# Runtime pins for the dashboard image and in-container reporting.
# Transitive packages resolve from these; pip-audit in CI scans the tree.
fastapi==0.141.1
uvicorn==0.34.0
httpx==0.28.1
reportlab==4.2.5

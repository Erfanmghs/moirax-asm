{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://recon.local/schemas/port-check.schema.json",
  "title": "port-check data.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "module", "results", "unreachable"],
  "properties": {
    "schema_version": { "type": "integer", "const": 1 },
    "module": { "type": "string", "const": "port-check" },
    "results": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["host", "ip", "ports"],
        "properties": {
          "host": { "type": "string" },
          "ip": { "type": "string" },
          "ports": {
            "type": "array",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["port", "proto", "state"],
              "properties": {
                "port": { "type": "integer", "minimum": 1, "maximum": 65535 },
                "proto": { "type": "string" },
                "state": { "type": "string", "const": "open" }
              }
            }
          }
        }
      }
    },
    "unreachable": {
      "type": "array",
      "items": { "type": "string" }
    }
  }
}
